language: python 

python:
  - "3.7"

cache: pip 

before_install: 
  - pip install -U pip
  - pip intall -U pytest
  - pip install codecov 

install:
  - pip install -r requirements.txt

script: pytest

after_success:
  - codecov
